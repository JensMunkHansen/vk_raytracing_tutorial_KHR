--- a/nvvkglsl/glsl.hpp
+++ b/nvvkglsl/glsl.hpp
@@ -147,7 +147,8 @@ public:
   void defaultTarget()
   {
     m_compilerOptions->SetTargetSpirv(shaderc_spirv_version::shaderc_spirv_version_1_6);
-    m_compilerOptions->SetTargetEnvironment(shaderc_target_env_vulkan, shaderc_env_version_vulkan_1_4);
+    // Use Vulkan 1.3 target for compatibility with older shaderc libraries
+    m_compilerOptions->SetTargetEnvironment(shaderc_target_env_vulkan, shaderc_env_version_vulkan_1_3);
   }

   // Sets the most typical compilation options. Note that without this, the
--- a/nvgpu_monitor/nvml_monitor.cpp
+++ b/nvgpu_monitor/nvml_monitor.cpp
@@ -88,8 +88,10 @@ static const std::string brandToString(nvmlBrandType_t brand)
       return "NVIDIA Virtual Compute Server";
     case NVML_BRAND_NVIDIA_VWS:
       return "NVIDIA RTX Virtual Workstation";
+#ifdef NVML_BRAND_NVIDIA_CLOUD_GAMING
     case NVML_BRAND_NVIDIA_CLOUD_GAMING:
       return "NVIDIA Cloud Gaming";
+#endif
     case NVML_BRAND_QUADRO_RTX:
       return "Quadro RTX";
     case NVML_BRAND_NVIDIA_RTX:
